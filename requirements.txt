# AlphaGPT-Routine 依赖（已在 macOS arm64 + Python 3.9 上验证通过）
# 安装: python3 -m venv .venv && .venv/bin/python -m pip install -r requirements.txt

torch==2.8.0
pandas==2.3.3
numpy==2.0.2
pyarrow==21.0.0
matplotlib==3.9.4
tqdm==4.70.1
requests==2.32.5
akshare==1.18.88
baostock==0.9.4
