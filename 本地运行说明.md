# 本地运行说明（macOS / 已配置完成）

本项目已在本机完成下载、依赖安装与配置，开箱即可运行。

- 项目目录：`/Users/duoxibao/Documents/ChatGPT/新策略/AlphaGPT-Routine`
- 虚拟环境：`.venv`（Python 3.9.6，已安装 torch 2.8.0 / pandas / akshare / baostock 等）
- 依赖清单：`requirements.txt`
- 实际配置：`config.env`（改这里即可，已加入 .gitignore，不会被提交）
- 配置模板：`.env.example`（变量含义说明）

## 一、运行方式

```bash
cd /Users/duoxibao/Documents/ChatGPT/新策略/AlphaGPT-Routine

./run.sh                 # 用 config.env 的配置跑
./run.sh 600519          # 临时换标的
TRAIN_ITERATIONS=1 ./run.sh            # 只跑 1 轮（快速看结果）
FORCE_TRAIN=False ./run.sh             # 复用已保存的公式，不重新训练
```

`run.sh` 会自动加载 `config.env`、设置 matplotlib 缓存目录，并调用 `.venv` 里的 Python。
也可以完全手动执行：

```bash
set -a; source config.env; set +a
MPLCONFIGDIR=/tmp/mpl_config .venv/bin/python times_astock.py
```

## 二、主要配置项（config.env）

| 变量 | 当前值 | 说明 |
| :--- | :--- | :--- |
| `INDEX_CODE` | `600519` | 标的代码。⚠️ 两融缓存只有沪市，深市（000/002/300）两融因子会全为 0 |
| `START_DATE` / `END_DATE` | `20220101` / `20261231` | 训练区间（YYYYMMDD） |
| `TRAIN_ITERATIONS` | `100` | 策略发现轮数；只做每日信号可设 `1` |
| `BATCH_SIZE` | `1024` | 批大小，机器慢可降到 `256` |
| `MAX_SEQ_LEN` | `10` | 公式最大复杂度 |
| `FORCE_TRAIN` | `True` | `True` 每次重训；`False` 复用已有 `*_best_formula_*.txt` |
| `ONLY_LONG` | `True` | A 股做多市场保持 `True` |
| `COST_RATE` | `0.0004` | 交易成本（印花税 + 手续费） |
| `LAST_NDAYS` | `42` | 报表展示最近多少个交易日 |
| `HOLD_PERIOD` | `11` | 持仓周期，必须 ≥ 2 |
| `BEST_FORMULA` | 空 | 填入公式则跳过训练，直接用该公式回测 |
| `CODE_FORMULA` | 未设置 | 多标的批量模式，格式 `代码:公式`，可多行 |
| `DINGTALK_WEBHOOK` / `DINGTALK_SECRET` | 空 | 钉钉机器人地址 / 加签密钥，留空则不推送 |

## 三、运行产物（生成在项目根目录）

| 文件 | 内容 |
| :--- | :--- |
| `<代码>_best_formula_<时间戳>.txt` | 训练得到的最优公式（Token + 可读形式） |
| `<代码>_metrics_<时间戳>.txt` | 样本外回测指标（收益 / 夏普 / 回撤 / 胜率） |
| `strategy_performance.png` | 策略净值曲线图 |
| `margin_balance/YYYYMMDD_margin_data.parquet` | 按日缓存的两融数据（缺失时自动抓取补全） |

## 四、已验证与你需要知道的几点

1. **数据源**：优先用 akshare（东方财富），失败自动回退 baostock。本机 akshare 请求会被本地代理（127.0.0.1:7890）拦断，实测由 baostock 兜底成功取数；如果你开了 Clash 等代理，可在终端里 `unset http_proxy https_proxy all_proxy` 后再跑，akshare 通常也能通。
2. **两融缓存只覆盖沪市**：`margin_balance/` 共 2347 个交易日（2017-01-25 ~ 2026-09-23），标的代码均为 `60/68/51/56/58` 开头。用深市股票时特征 `F_BUY_F_REPLAY` 恒为 0，结论要打折。
3. **计算设备是 CPU**：脚本只判断 CUDA，Mac 上不会走 MPS（本项目当前也不支持）。`TRAIN_ITERATIONS=100` 在本机约需数分钟，追求速度可降 `BATCH_SIZE` 或轮数。
4. **matplotlib 缓存**：系统禁止写入 `~/.matplotlib`，`run.sh` 已把 `MPLCONFIGDIR` 指向 `/tmp/mpl_config`。

## 五、在 GitHub Actions 上跑（可选）

仓库已带 `.github/workflows/astock_analysis.yml`：每天 UTC 08:13 自动跑一次，并把新抓的两融数据
提交回仓库。要启用，把本项目推到自己的 GitHub 仓库，然后在
`Settings → Secrets and variables → Actions` 里配置：

- Secrets：`DINGTALK_WEBHOOK`、`DINGTALK_SECRET`
- Variables：`INDEX_CODE`、`TRAIN_ITERATIONS`（日常建议 `1`）、`FORCE_TRAIN`（日常 `False`）、`ONLY_LONG=True`

> 免责声明：本项目为量化研究/教学工具，回测结果不代表未来收益。
