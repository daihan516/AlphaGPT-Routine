# AlphaGPT-Routine v2（A股横截面优化版）

对照原始项目 `imbue-bit/AlphaGPT` 的核心原理，对 `kafroc/AlphaGPT-Routine`（下称 v1）做的一次系统性重写。
新代码在 `times_astock_v2.py`，**v1 的 `times_astock.py` 保持原样不动**，方便对照。

---

## 一、保留的「原始版核心原理」

AlphaGPT 的灵魂不是 Transformer，而是这四条：

| 原理 | 原始版做法 | v2 的做法 |
|---|---|---|
| ① 闭环自进化 | 生成公式 → VM 执行 → 回测打分 → REINFORCE 更新 | 完全保留 |
| ② **奖励函数即风控** | Sortino 为主轴 + 交易次数下限 + 大回撤二次惩罚 + 流动性过滤 + 中位数抗异常 | 全部落地（详见第二节） |
| ③ **横截面训练** | 一次在 **500 个代币**上评估同一个公式 | 一次在 **12 只股票**上评估（v1 只训练 1 只股票） |
| ④ 严格样本外 + 可解释公式 | 80/20 切分，波兰式 token + 合法掩码 | 完全保留 |

---

## 二、修掉的三个致命问题（都有实测证据）

### 1. 第 6 个因子未归一化 —— 量级是其他因子的 1 亿倍

实测 v1 的因子量级：

| 因子 | std | 范围 |
|---|---|---|
| RET / RET5 / VOL_CHG / V_RET / TREND | 1.6 ~ 2.3 | **±5**（已 robust_norm） |
| **F_BUY_F_REPLAY** | **1.9e8** | **±11.7 亿**（未归一化） |

后果：任何用到该因子的公式都完全被它支配，`tanh` 直接饱和 → 信号退化成"永远满仓/永远空仓"。
而随机搜索有 1/6 概率选到它，**整个搜索基本是退化的**。

**v2 修复**：改成「融资净买入 / 融资余额」的相对量（量纲无关），并纳入统一的 `robust_norm`。

### 2. 奖励函数被改成「胜率最大化」

| | 原始版 | v1 | v2 |
|---|---|---|---|
| 奖励 | Sortino 比率 + 多重风控惩罚 | **胜率百分比** | 日尺度年化**索提诺** + 交易次数下限 + 暴露下限 + 回撤惩罚 + 复杂度惩罚 |

胜率是可以靠"赚小钱、扛大亏"刷高的危险目标（v1 日志里那个 `BestSortino: 84.21` 其实就是 **84.21% 的胜率**，变量名没改）。
**v2 恢复原始版的"风险调整收益"思路**，并把 4 条风控全部编码进奖励：

1. 单只标的交易次数 `< MIN_TRADES`（默认 12）→ 判无效
2. 仓位暴露 `< 10%` → 判无效（防止"几乎不持仓 + 低波动"刷出虚高比率）
3. 最大回撤超过 25% 的部分 × 3 倍惩罚
4. 算子越多扣分越多（原版注释原话："短小精悍的公式往往更稳"）

### 3. 重叠收益被当成日收益 —— 年化虚增

v1 的收益标签是「未来 2~11 天里第一个正收益的开盘价卖出」，
于是**每天一个信号、持有 11 天 → 11 个重叠的多日收益被当作 11 个独立日收益做复利**。

实测对照（同一公式 `SUB(NEG(TREND),RET)`、同一标的 600519、同一区间 2025-10-21 ~ 2026-09-24、持有期都取 11 天）：

| 口径 | 年化收益 | 夏普 | 最大回撤 |
|---|---|---|---|
| **v1 报告值** | **+54.75%** | **1.688** | 33.51% |
| **v2 诚实口径** | **−15.66%** | **−0.88** | 21.92% |
| 同期买入持有（基准） | −12.99% | −0.67 | 23.07% |

**同一个公式、同样的数据，v1 报 +54.75%，诚实口径是 −15.66%，相差 70 个百分点。**
（v1 自己的持仓明细表当时显示 42 天复合 −4.97%，其实已经在"打脸"它的 reality check 了。）

**v2 修复**：改成**持仓状态机**——
- 持仓期间不开新仓（收益天然不重叠）
- 日频净值曲线 mark-to-market，年化按 252 交易日
- 每笔交易的净收益与净值曲线**严格一致**（已用 5 个随机种子 × 4 个持有期 × 200 条序列与参考实现逐日比对通过）

---

## 三、A股制度适配

| 制度 | v1 | v2 |
|---|---|---|
| T+1 | 隐含在 k≥2，但持仓可重叠 | 明确：买入次日才可卖 |
| 涨停 | 无（按开盘价随便成交） | **开盘涨停买不进**，放弃该信号 |
| 跌停 | 无 | **开盘跌停卖不出**，顺延到下一个可成交日 |
| 停牌/零成交 | 无 | 跳过，不参与交易 |
| 涨跌停比例 | 无 | 主板 10% / 创业板·科创板 20% / 北交所 30%（ST 用 `LIMIT_RATE=0.05`） |
| 交易成本 | 单边 0.04%（不分买卖） | 佣金 0.025% + 过户费 0.001% + **印花税 0.05%（仅卖出）** + 滑点 0.05% → **往返 0.202%** |

---

## 四、v2 的横截面改造（最关键的升级）

原始版一次在 **500 个代币**上评估同一个公式，v1 只在 **1 只股票**上训练。
v1 的样本量：1 × 917 天；原始版：500 × 10,080 根 1 分钟 K 线。
**差约 360 倍**，在这个样本量上做强化学习搜索必然过拟合，所以 v2 补上了这一步：

```
UNIVERSE（12 只沪市两融标的）
   → 对齐到共同交易日（1147 天）
   → 因子矩阵 [12 标的, 6 因子, 1147 天]
   → 一个公式同时作用在 12 只标的上
   → 每只标的独立模拟持仓
   → 分数 = 12 只标的索提诺的【中位数】（抗异常值，原版用 torch.median）
   → 再加上"达标标的比例"门槛（默认 ≥60% 的标的必须够交易次数和暴露）
```

配套的工程改造：
- `simulate_batch`：向量化持仓模拟器，一次并行模拟上万条序列（比逐条循环快 20 倍以上），已与循环参考实现逐日比对一致
- 固定随机种子 `SEED=42`，同一个配置跑两次结果一致（v1 每次结果都不同，无法复现）
- 两融数据一次性构建 `[日期 × 标的]` 矩阵并缓存，避免逐日重复读 1147 个 parquet

---

## 五、实测结果（v2，12 只沪市标的，200 轮搜索）

**训练段**：2022-01-04 ~ 2025-10-20（917 天）
**样本外**：2025-10-21 ~ 2026-09-24（230 天）

发现的最优公式：

```
JUMP(MUL(VOL_CHG,MAX3(V_RET)))
```

含义：成交量变化 × 量价收益的近 3 日最大值，再做极端跳变检测（z-score > 3）
→ 即**只捕捉"量价异动"这种极端事件**，平时空仓。

| 指标 | v2 等权组合 | 等权买入持有 |
|---|---|---|
| 累计收益 | **−0.76%** | −1.89% |
| 年化收益 | −0.83% | −2.07% |
| 年化波动 | **4.38%** | 15.10% |
| 最大回撤 | **4.44%** | 15.73% |
| 样本外交易 | 58 笔 | — |
| 整体胜率 | 48.3% | — |

**怎么解读**：这是一个**低波动防御型**结果——在下跌市里亏得更少、回撤只有基准的 1/3，但**没有跑出正 alpha**。
这就是诚实口径下的真实答案：这类自动因子挖掘在"12 只股票 × 4 年日线"这个样本量上，还不足以稳定挖出超额收益。

---

## 六、已知局限（下一步的高杠杆方向）

1. **样本量仍然太小**：12 只 × 917 天，与原始版的 500 万根 K 线差 360 倍。**扩池是最有效的下一步**（比如沪深 300 成分股）。
2. **两融缓存只有沪市**：深市标的（000/002/300）的 `MARGIN_NET` 因子恒为 0。要补齐需抓 `stock_margin_detail_szse`。
3. **模型仍是朴素 Transformer**：原始版主线用的是 LoopedTransformer + QKNorm + SwiGLU + MTP 多任务头 + **LoRD 低秩正则**，v2 还没有移植。
4. **单公式、无组合优化**：没有仓位分配、行业中性、风险平价等。
5. **日线级别**：原始版是 1 分钟线的高频逻辑，日线上因子的信息量小很多。

---

## 七、怎么用

```bash
cd /Users/duoxibao/Documents/ChatGPT/新策略/AlphaGPT-Routine

# 默认：12 只沪市池，训练 100 轮
MPLCONFIGDIR=/tmp/mpl_config .venv/bin/python times_astock_v2.py

# 快速验证（不训练，用指定公式直接回测）
CODE_FORMULA="600519:SUB(NEG(TREND),RET)" TRAIN_ITERATIONS=1 \
  .venv/bin/python times_astock_v2.py

# 自定义标的池与持有期
UNIVERSE=600519,600036,601318 HOLD_PERIOD=10 TRAIN_ITERATIONS=200 \
  .venv/bin/python times_astock_v2.py

# 复用上次训练出的公式（不重新搜索）
FORCE_TRAIN=False .venv/bin/python times_astock_v2.py
```

产物：`<代码>_best_formula_v2_<时间戳>.txt`、`strategy_performance_v2.png`、`margin_universe_cache.parquet`（两融矩阵缓存）。

钉钉推送内容升级为：**下一交易日买入名单**（池内哪些标的触发信号）+ 样本外等权组合表现 + 公式。

---

## 八、免责声明

本项目为量化研究/教学工具。所有回测均为单公式、单标的池、日线级别，不含组合优化与仓位管理，
历史回测不代表未来收益。股市交易风险极高，请勿直接据此实盘。
